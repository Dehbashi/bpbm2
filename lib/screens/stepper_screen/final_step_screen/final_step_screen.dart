import 'package:flutter/material.dart';

class FinalStepScreen extends StatelessWidget {
  const FinalStepScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Final Step screen'),
    );
  }
}

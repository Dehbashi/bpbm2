import 'package:flutter/material.dart';

class UserNewAddressScreen extends StatelessWidget {
  const UserNewAddressScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('User New Address Screen'),
    );
  }
}
